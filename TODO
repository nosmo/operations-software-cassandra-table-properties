TODO
====

schema/master.json
------------------

min_sstable_size is defined as integer. 
Needs to be regex to accept number in bytes and for example 10MB

compaction_window_unit: what are the units for this?

sstable_size_in_mb  is defined as integer. 
Needs to be regex to accept number in bytes and for example 10MB ????

Warnings : dclocal_read_repair_chance table option has been deprecated and will be removed in version 4.0

add schema validation

make comparison opaque

Replace key_mapper with AS name in query statement

Make the schema validation error reporting more user friendly

Make tables not required in schema?
